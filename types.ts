
export interface Project {
  id: string;
  title: string;
  desc: string;
  img: string;
  tags: string[];
  date: string;
}

export interface Blog {
  id: string;
  title: string;
  desc: string;
  url: string;
  date: string;
}